# TODO — 給在 Mac 上接手的 Claude

這是 Jack 的求職履歷網站（純 HTML/CSS，無框架）。你的任務只有一個：
**補完 §1 工作經歷的內容**。動手前先讀 `CLAUDE.md`（已拍板的決策與禁區）。

## 唯一任務：補沃草工作經歷

位置：`index.html` 裡 `id="work"` 區塊，找這段：

```html
<!-- TODO(Jack): 從 Mac 補上實際負責的系統與專案 ... -->
<ul class="job__points">
  <li class="todo">主要負責系統與專案內容待補（由本人補充）</li>
</ul>
```

做法：
1. 跟 Jack 訪談他在沃草四年實際負責的系統與專案（Mac 上應有工作素材可參考）。
2. 整理成 **2–5 條 `<li>`**，每條格式：「做了什麼 → 用什麼技術 → 帶來什麼結果/規模」。
   範例：`<li>重構 XX 系統的資料管線（Python/PostgreSQL），將日更流程從人工 2 小時降為全自動。</li>`
3. 刪掉 `class="todo"` 那條 placeholder 和上方的 TODO 註解。
4. 若有可公開的代表性專案（如沃草的公開產品），可在 `job__desc` 提及名稱。

## 邊界（不要越過）

- **保密**：公司內部細節只寫到 Jack 點頭可公開的程度，逐條唸給他確認。
- **不要動** §2 個人系統、§3、§4、聯絡區——那些是本機 Claude 的職責範圍。
- **不要**加照片、語言能力、電話號碼（決策見 CLAUDE.md，Jack 已拍板）。
- 風格：跟現有文案一致（繁中、精準、不浮誇；「結果」盡量量化）。

## 完成後

1. 瀏覽器直接開 `index.html` 確認排版沒破（純靜態，不用起 server）。
2. commit + push 到 origin（GitHub Pages 會自動更新）。
3. 把這個檔案裡「唯一任務」段落刪除或標記完成。

## 之後的事（不歸你，列著備忘）

- 自訂網域：等 Jack 想好要不要買（.dev/.com/.tw），綁上 GitHub Pages 即可。
- hero 的 `jack-sun.service` 是裝飾字串，買了 domain 後可換成真網域。
