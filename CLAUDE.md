# CLAUDE.md — Jack 履歷網站

給未來接手的 Claude 的工作備忘。設計說明見 `README.md`。

## 目前狀態（2026-07-10）

v1 完成：單頁、純 HTML/CSS、四個 case study、全區塊實測截圖驗證過（桌機＋手機）。
尚未部署、尚未 commit 初版以外的內容。

## 已拍板的決策（2026-07-10 與 Jack 確認）

- **用途**：求職（未來用），非接案、非個人品牌。
- **語言**：純繁體中文。**不寫語言能力**（英日文僅聽說，Jack 明確說先不放）。
- **身分**：英文名「Jack」、**不放照片**、聯絡只放 email＋電話。
- **接案作品**：無（幫別人做的網站不在展示範圍）。
- **策展原則**：pikmin-gps、IG 自動化等灰色專案**永不上站**；
  stock-monitor 以「研究平台」呈現，**不得出現任何實際財務數字**。
- **絕對禁止**：任何個人敏感資訊（參見 memory `user_jack.md`）不得出現在這個公開網站上。

## 待 Jack 補充（下次接手先問）

1. **沃草工作內容**：§1 的 bullet 是 TODO placeholder，Jack 說會從他的 Mac
   整理實際負責的系統/專案來補。格式建議「做了什麼 → 技術 → 結果」。
2. **顯示名稱**：目前只寫「Jack」。求職網站通常要全名（如 Jack + 姓氏拼音），
   等 Jack 決定正式英文名。hero 狀態列的 `jack.systems` 是虛構裝飾字串，
   若之後買的 domain 不同記得同步改。
3. **網域**：尚未購買。建議等正式英文名定案後再挑（.dev/.com/.tw）。
   先部署 GitHub Pages，之後綁 domain 零重工。
4. **model-audit 條目**：§3 有一條「測試中國語言模型審查邊界」的研究專案，
   我判斷對 civic-tech 求職是加分項所以放了，但 Jack 尚未明確點頭——問一下，
   他若不要就刪。

## 開發備忘

- 預覽：`python3 -m http.server 8792`。
- 視覺驗證：用 `~/.cache/ms-playwright/chromium-*` + playwright python 套件，
  真實 `scrollIntoView` 逐段截圖（別用超高視窗一次截，hero 沒有 100vh 但養成習慣）。
- 強調詞 `em` 是墨字＋橘 highlight 底帶；別再把字也改橘（試過，太重）。
- 稿紙橫線背景已調到 45% 透明度，再深會搶內文。
